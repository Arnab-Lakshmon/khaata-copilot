"use client";

import { useEffect, useState } from "react";

type Invoice = { id: string; invoice_number: string; party_name: string; amount: number | string; due_date: string; status: "paid" | "partial" | "unpaid" };
type Health = { score: number; collectionRate: number; reconciliationRate: number; bookkeepingRate: number; invoiceList: Invoice[]; weights: { collection: number; reconciliation: number; bookkeeping: number } };
function money(value: number | string) { return new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(Number(value)); }
function date(value: string) { return new Intl.DateTimeFormat("en-IN", { day: "numeric", month: "short", year: "numeric" }).format(new Date(value)); }
function pct(value: number) { return `${Math.round(value * 100)}%`; }
function scoreColor(score: number) { return score < 40 ? "#d85b3f" : score < 70 ? "#c58b2d" : "#388452"; }

export default function HealthPage() {
  const [health, setHealth] = useState<Health | null>(null); const [error, setError] = useState(""); const [editingId, setEditingId] = useState<string | null>(null); const [draftDate, setDraftDate] = useState(""); const [savingId, setSavingId] = useState<string | null>(null);
  async function refresh() { try { const response = await fetch("/api/health", { cache: "no-store" }); const body = await response.json(); if (!response.ok) throw new Error(body.error); setHealth(body); setError(""); } catch (reason) { setError(reason instanceof Error ? reason.message : "Could not load shop health."); } }
  useEffect(() => { refresh(); const interval = window.setInterval(refresh, 15000); return () => window.clearInterval(interval); }, []);
  function beginEdit(invoice: Invoice) { setEditingId(invoice.id); setDraftDate(invoice.due_date.slice(0, 10)); setError(""); }
  async function saveDueDate(invoiceId: string) { setSavingId(invoiceId); try { const response = await fetch("/api/health", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ invoiceId, dueDate: draftDate }) }); const body = await response.json(); if (!response.ok) throw new Error(body.error); setEditingId(null); await refresh(); } catch (reason) { setError(reason instanceof Error ? reason.message : "Could not update due date."); } finally { setSavingId(null); } }
  const score = health?.score || 0; const color = scoreColor(score); const circumference = 2 * Math.PI * 52;
  return <main className="min-h-screen bg-[#f4f0e8] px-6 py-8 text-[#17211d] sm:px-10 lg:px-16"><div className="mx-auto max-w-6xl"><a href="/" className="font-satoshi text-sm font-bold tracking-[0.16em] uppercase">← Khaata Copilot</a><header className="mt-16 flex flex-col justify-between gap-8 border-b border-[#17211d]/15 pb-10 lg:flex-row lg:items-end"><div><p className="font-satoshi text-sm font-bold tracking-[0.16em] text-[#d85b3f] uppercase">Shop health</p><h1 className="font-tanker mt-4 text-6xl leading-none sm:text-8xl">Know what needs care.</h1><p className="font-satoshi mt-6 max-w-xl text-lg leading-8 text-[#17211d]/65">One view of collections, payment matching, and the rhythm of your bookkeeping.</p></div><div className="flex items-center gap-6 rounded-[1.5rem] border border-[#17211d]/15 bg-white/60 p-5"><div className="relative h-36 w-36"><svg viewBox="0 0 120 120" className="h-full w-full -rotate-90"><circle cx="60" cy="60" r="52" fill="none" stroke="#17211d" strokeOpacity=".1" strokeWidth="10"/><circle cx="60" cy="60" r="52" fill="none" stroke={color} strokeLinecap="round" strokeWidth="10" strokeDasharray={circumference} strokeDashoffset={circumference * (1 - score / 100)} className="transition-[stroke-dashoffset] duration-1000 ease-out"/></svg><span className="font-tanker absolute inset-0 flex items-center justify-center text-5xl">{health ? score : "—"}</span></div><div><p className="font-satoshi text-xs font-bold tracking-[0.14em] text-[#17211d]/50 uppercase">Health score</p><p className="font-satoshi mt-2 max-w-[10rem] text-sm text-[#17211d]/65">{health ? score >= 70 ? "The shop is in a healthy rhythm." : score >= 40 ? "A few areas need attention." : "Collections and records need care." : "Calculating from your shop data…"}</p></div></div></header>{error && <p className="font-satoshi mt-8 font-bold text-[#d85b3f]">{error}</p>}{health && <><section className="mt-10 grid gap-4 sm:grid-cols-3">{[["Collection rate", health.collectionRate, "50% weight"],["Reconciliation", health.reconciliationRate, "30% weight"],["Bookkeeping ≤ 3 days", health.bookkeepingRate, "20% weight"]].map(([label, value, weight]) => <article key={String(label)} className="rounded-2xl border border-[#17211d]/15 bg-white/55 p-5"><p className="font-satoshi text-xs font-bold tracking-[0.12em] text-[#17211d]/50 uppercase">{label}</p><p className="font-tanker mt-3 text-5xl">{pct(Number(value))}</p><p className="font-satoshi mt-2 text-sm text-[#17211d]/55">{weight}</p></article>)}</section><section className="mt-12 rounded-[2rem] border border-[#17211d]/15 bg-white/55 p-5 shadow-[6px_6px_0_#17211d] sm:p-8"><div className="flex flex-wrap items-end justify-between gap-4 border-b border-[#17211d]/15 pb-6"><div><p className="font-satoshi text-xs font-bold tracking-[0.14em] text-[#17211d]/50 uppercase">Accounts receivable</p><h2 className="font-satoshi mt-2 text-2xl font-bold">All invoices</h2></div><p className="font-satoshi text-sm text-[#17211d]/60">Auto-refreshes every 15 seconds</p></div><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[680px] border-collapse text-left font-satoshi"><thead><tr className="border-b border-[#17211d]/10 text-xs font-bold tracking-[0.1em] text-[#17211d]/50 uppercase"><th className="py-4 pr-4">Invoice</th><th className="py-4 pr-4">Party</th><th className="py-4 pr-4">Amount</th><th className="py-4 pr-4">Due date</th><th className="py-4">Status</th></tr></thead><tbody>{health.invoiceList.map((invoice) => <tr key={invoice.id} className="border-b border-[#17211d]/10 last:border-0"><td className="py-5 pr-4 font-bold">{invoice.invoice_number}</td><td className="py-5 pr-4">{invoice.party_name}</td><td className="py-5 pr-4 font-bold">{money(invoice.amount)}</td><td className="py-5 pr-4">{editingId === invoice.id ? <div className="flex items-center gap-2"><input type="date" value={draftDate} onChange={(event) => setDraftDate(event.target.value)} className="rounded-lg border border-[#17211d]/25 bg-white px-2 py-1 text-sm outline-none focus:border-[#d85b3f]"/><button onClick={() => saveDueDate(invoice.id)} disabled={savingId === invoice.id} className="rounded-full bg-[#17211d] px-3 py-1 text-xs font-bold text-white disabled:opacity-50">{savingId === invoice.id ? "Saving…" : "Save"}</button><button onClick={() => setEditingId(null)} className="text-xs font-bold text-[#17211d]/55">Cancel</button></div> : <button onClick={() => beginEdit(invoice)} className="group inline-flex items-center gap-2 rounded px-1 py-1 text-left hover:bg-white/70 focus-visible:outline focus-visible:outline-2 focus-visible:outline-[#d85b3f]" aria-label={`Edit due date for ${invoice.invoice_number}`}><span>{date(invoice.due_date)}</span><span aria-hidden="true" className="text-[#17211d]/45 transition-colors group-hover:text-[#d85b3f]">✎</span></button>}</td><td className="py-5"><span className={`rounded-full px-3 py-1 text-xs font-bold ${invoice.status === "paid" ? "bg-[#d8ead7] text-[#23613a]" : invoice.status === "partial" ? "bg-[#f1d5a5] text-[#795615]" : "bg-[#f4d8d0] text-[#a13c28]"}`}>{invoice.status}</span></td></tr>)}</tbody></table>{!health.invoiceList.length && <p className="font-satoshi py-8 text-sm text-[#17211d]/60">No invoices found for the demo shop.</p>}</div></section></>}</div></main>;
}
